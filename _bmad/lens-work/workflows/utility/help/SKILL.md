---
name: lens-work-help
description: "Show available commands and usage reference. Use when asking for command help."
---

Follow the instructions in ./workflow.md.

> **`/help` vs `/bmad-help`:** `/help` shows the LENS Workbench command registry (this workflow). `/bmad-help` is a cross-module meta-command provided by the `core` module — it offers general BMAD platform guidance and is not LENS-specific.
